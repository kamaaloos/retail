/.gradle/
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Keep the Gradle wrapper so the project builds after clone/upload.
# (Do not ignore gradlew, gradlew.bat, or gradle-wrapper.jar.)
